function squareOfStars(size = 5){
    for (let i = 0; i < size; i++) {
        let row = '';

        for (let y = 0; y < size; y++) {
            row += '* ';
        }
        row = row.trim();
        console.log(row);
    }
}
squareOfStars();
console.log(`\n --- \n`);
squareOfStars();
