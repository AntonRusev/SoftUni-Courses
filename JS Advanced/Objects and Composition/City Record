function cityRecord(name, population, treasury) {
    const obj = {
        name,
        population,
        treasury
    };
   return obj;
}
cityRecord('Tortuga',
7000,
15000);
console.log(`\n --- \n`);
cityRecord('Santo Domingo',
12000,
23500);
