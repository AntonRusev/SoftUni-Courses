function processOddPositions(array) {
    let result = array.filter((_, i) => i % 2).map(x => x * 2).reverse().join(' ');
    console.log(result);
}
processOddPositions([10, 15, 20, 25]);
console.log(`\n --- \n`);
processOddPositions([3, 0, 10, 4, 7, 3]);
