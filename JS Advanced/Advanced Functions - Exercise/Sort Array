function sortArray(numbers, sorting){
    if (sorting == 'asc') {
        numbers.sort((a, b) => a - b);
    } else {
        numbers.sort((a, b) => b - a)
    }
    return numbers;
}
sortArray([14, 7, 17, 6, 8], 'asc');
console.log(`\n --- \n`);
sortArray([14, 7, 17, 6, 8], 'desc');
