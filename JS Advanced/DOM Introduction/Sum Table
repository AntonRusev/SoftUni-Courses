function sumTable() {
    const firstTable = Array.from(document.getElementsByTagName('tr')).slice(1, -1);
    let totalSum = 0;
    for (let element of firstTable) {
        totalSum += Number(element.lastElementChild.textContent);
    }
    document.getElementById('sum').textContent = totalSum;
}
