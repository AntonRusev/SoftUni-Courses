function matrix(num) {

    for (let i = 0; i < num; i++) {
        let row = '';
        for (let y = 0; y < num; y++) {
            row += num + " ";
        }
        console.log(row)
    }
}
matrix(3);
console.log(`\n --- \n`);
matrix(7);
console.log(`\n --- \n`);
matrix(2);
