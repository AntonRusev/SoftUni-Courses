function repeatString(string, repeats) {
    let result = '';

        for (i = 0; i < repeats; i++) {
            result += string;
        }
      return result;

}
repeatString("abc", 3);
repeatString("String", 2);
