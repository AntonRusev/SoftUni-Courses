function processOfNumbers(arr) {
    let result = arr
    .filter((num, i) => i % 2 == 1)
    .map(x => x * 2)
    .reverse()

    console.log(result.join(" "));
}
processOfNumbers([10, 15, 20, 25]);
console.log(`\n --- \n`);
processOfNumbers([3, 0, 10, 4, 7, 3]);
